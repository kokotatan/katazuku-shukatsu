{
  "name": "katazuku-shukatsu",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "build": "npm --prefix board install && npm --prefix board run build && npm --prefix sync install && npm --prefix sync run check && npm --prefix sync run check:db",
    "check": "npm --prefix sync run check && npm --prefix sync run check:db"
  }
}
