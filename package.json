{
  "name": "katazuku-shukatsu",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "npm --prefix inbox install && npm --prefix inbox run build && npm --prefix status install && npm --prefix status run build && npm --prefix insight install && npm --prefix insight run build && npm --prefix profile install && npm --prefix profile run build && npm --prefix prep install && npm --prefix prep run build && node scripts/assemble.mjs"
  }
}
