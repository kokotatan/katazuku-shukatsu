# Security & Safety Policy

このプロジェクトは応募という外部への確定操作を扱うため、安全境界を最初から明文化します。
セキュリティと安全境界の両方をここに置きます。

## 安全境界(コードで守る一線)

自動運転であっても、本人の代わりに越えてはいけない一線があります。これは注意書きではなく、
DB API・CLI・ブラウザ操作の各層で強制する設計です。

- **ES・エントリーの最終送信は本人の明示承認を必須**にする。
  - 環境変数 `KATAZUKU_APPROVAL_SECRET` を設定すると、承認は**偽造不能なトークン**(`createApprovalToken`)の
    検証になる。シークレットを見られないモデル/プロバイダはトークンを作れないため、承認を偽造できない。
    未設定時は簡易な真偽値フォールバック(ローカル用途)。運用では設定を推奨。
- **最小権限**: read 系 capability は読み取り専用ツールにのみ解決し、送信・変更ツールへは広げない
  (connector全体のワイルドカードを read に渡さない)。子プロセスへ渡す環境も最小化する。
- **本人認証・CAPTCHA・SMS・メール認証を回避しない**
- **本番のWeb適性検査・コーディングテストを代理受験しない**。問題・解答・検査画面を収集しない
- 面接予約は本人が事前指定した空き条件の範囲だけ自動化する
- すべての外部確定操作に**冪等キーと監査ログ**を持たせ、二重送信・二重予約を防ぐ
- 辞退・削除など後戻りできない操作は、別の強い確認を要求する
- 接続先ごとに利用規約・アクセス頻度・robots・API規約を確認する

## 個人データを持ち込まない

- 実データ(氏名・住所・学校・連絡先・顔写真・ES本文・面接記録・メール本文)はリポジトリに含めません
- APIキー・合言葉・サービスアカウント鍵は環境変数またはOSのキーチェーンで扱い、コミットしません
- テスト・サンプルのフィクスチャはすべて架空です
- 正本DBと非公開写真は、既定でリポジトリ外のOSユーザーデータ領域に保存します
- `.gitignore` だけに依存せず、`npm run scan` でGit追跡対象のSQLiteをヘッダと拡張子から検出します
- DBを誤ってpushした場合は、ファイルを消すだけでなくGit履歴から除去し、DB内にあった認証情報を失効・変更してください

## 対象バージョン

まだ 0.x の初期段階です。修正は最新の `main` に対して行います。

| バージョン | サポート |
|---|---|
| 0.1.x / main | あり |
| それ以前 | なし |

## 脆弱性の報告

**公開Issueにはしないでください。** 次のいずれかで非公開に連絡してください。

1. GitHub の **Private Vulnerability Reporting**(このリポジトリの Security タブ → "Report a vulnerability")。推奨。
2. それが使えない場合は、リポジトリ所有者(GitHub: [@kokotatan](https://github.com/kokotatan))へ GitHub 経由で非公開に連絡。

報告には、再現手順・影響範囲・想定される悪用を添えてください(実データ・個人情報は含めないこと)。
初回応答の目安は1週間以内です。修正の公開までは詳細の開示を控えてください。
