node_modules
dist
.vercel
.wrangler/
cloudflare/.dev.vars*
worker-configuration.d.ts
worker-configuration.d.ts.tmp

# Discord通知の設定・状態(Webhook URLは秘密情報)
notify-config.json
notify-state.json
notify-items-*.json

# 個人情報入りのローカルプロフィール
*.local.md

# 選考管理シート等から変換した取込データ(個人情報)
sheet-import-*.json
gmail-import-*.json
notes-export-*.json
profile-export-*.json
people-export-*.json
sync/research-*-loop.json
sync/application-*-loop.json
sync/mail-import-*.json
sync/submission-import-*.json
sync/calendar-import-*.json
sync/calendar-raw-events.json
# 取り込み系の実行時生成物は個人データを含むので、種別ごとの列挙漏れを防ぐため一括で除外する
sync/*-loop.json
sync/*-import-*.json
notify-state.json
prep/seed-example.json

# Googleサービスアカウントの鍵(秘密情報)
service-account.json

# APIキー等の環境変数(ANTHROPIC_API_KEY など。秘密情報)
.env
.env.local
.env.*.local

# Claude Code の端末固有許可設定
.claude/settings.local.json

# 自動同期の実行ログ
logs/

# 正本DB(SQLite。選考・企業の個人データ)とミラー出力
data/
mirror-out.json

# ブラウザ操作MCP(Playwright)のスクリーンショット等(顔写真・ES内容など個人情報が写る)
.playwright-mcp/
mitsui-*.png
sec-*.png
es-*.md
tl-snap-*.md

# ブラウザMCPからアップロードする一時ファイル(顔写真など個人情報)
tmp/

.vercel

# ローカル資格情報ブローカー（暗号化済みでも機密情報として扱う）
*.dpapi.json
credential-store/

# Codexのリポジトリ内個人設定(端末固有の絶対パスを含む)とsandbox setupバイナリ
.codex/
codex-windows-sandbox-setup.exe
.katazuku-satellite
.katazuku-emergency-canonical.local.json

# record-vac の二重起動ガード用ロック(実行時の一時ファイル)
logs/record-vac-*.lock

# private限定の遮断リスト(個人名・実社名を含む。OSSへは出さない)
scripts/oss/blocklist.txt
# 企業ごとの差分JSON(実企業の選考データ・個人情報)
sync/*-diff.json
# カレンダー突合の一時生成物
sync/.calendar-residue-tmp/
# Gmail障害時のローカル読取専用バックフィル（メール本文・ヘッダーを含む）
sync/gmail-backfill-*.json

# Google OAuthの秘密情報
/credentials/

# 端末ローカルの作業ファイル(実メール本文・使い捨てスクリプト・一時出力)。両機とも同じ規則にする
*.local.json
*.local.cjs
*.local.mjs
*.local.txt
*.local.env
.tmp/
output/
outputs/
%SystemDrive%/
NUL
*.pub
