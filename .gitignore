node_modules
dist
.vercel

# Discord通知の設定・状態(Webhook URLは秘密情報)
notify-config.json
notify-state.json
notify-items-*.json

# 個人情報入りのローカルプロフィール
*.local.md

# 選考管理シート等から変換した取込データ(個人情報)
sheet-import-*.json
gmail-import-*.json
notes-export-*.json
profile-export-*.json
prep/seed-example.json

# Googleサービスアカウントの鍵(秘密情報)
service-account.json

# APIキー等の環境変数(ANTHROPIC_API_KEY など。秘密情報)
.env
.env.local
.env.*.local

# 自動同期の実行ログ
logs/

# ブラウザ操作MCP(Playwright)のスクリーンショット等(顔写真・ES内容など個人情報が写る)
.playwright-mcp/
mitsui-*.png
sec-*.png
es-*.md
tl-snap-*.md

# ブラウザMCPからアップロードする一時ファイル(顔写真など個人情報)
tmp/
